/** Log-friendly errors. */
export * from './pity';
export { Info } from 'verror';
/** Asserts the input predicate. */
export declare function assert(pred: unknown, msg: string): asserts pred;
/** Asserts that the input value can't be instantiated. */
export declare function unreachable(val: never, msg?: string): never;
